#include <stdio.h>

/**
  * main - entry point
  * Description: print alphabet with putchar
  *
  * Return: 0
  */

int main(void)
{
	char alph;

	alph = 'a';
	while (alph <= 'z')
	{
		if (alph != 'q' && 'e')
		{
		putchar(alph);
		alph++;
		}
	}
	putchar(10);

	return (0);
}

