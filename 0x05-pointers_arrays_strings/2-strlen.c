/**
  * _strlen - calculate length of string pointed to
  * @s: address of string
  *
  * Return: length of string
  */

int _strlen(char *s)
{
	int a;

	for (a = 0; *(s + a) != \0; a++)
	{
	}
	return (a);
}
