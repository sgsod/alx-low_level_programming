#include <stdio.h>

/**
  * main - entry point
  * Description: print base 10 numbers
  *
  * Return: 0
  */

int main(void)
{
	char alph;

	alph = '0';
	while (alph <= '9')
	{
		putchar(alph);
		if (alph < '9')
		{
			putchar(',');
			putchar(' ');
		}
		alph++;
	}
	putchar(10);

	return (0);
}

